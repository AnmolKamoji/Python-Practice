a = input()
print(a)
# There are multiple specific data types that can be imported using the input function 
# just add the datatype before the input like s = int(input()) so this basically takes only integer inputs. 
# its the same for all other datatypes except the boolean datatype. For boolean it remains normal input()


# There are also various actions that you can perform on the inputs like the following:

